<section class="cta-section">
  <div class="cta-inner">
    <h2 class="cta-heading">开始你的下一段旅程</h2>
    <div class="cta-options">
      <a href="/contact" class="cta-card cta-card-primary">
        <span class="cta-label">定制专属旅程</span>
        <span class="cta-desc">与旅行顾问一对一深度沟通<br/>设计只属于你的行程方案</span>
        <span class="cta-arrow">与顾问对话 &rarr;</span>
      </a>
      <a href="/journeys" class="cta-card">
        <span class="cta-label">浏览已有行程</span>
        <span class="cta-desc">查看我们精心打磨的<br/>标杆旅程方案</span>
        <span class="cta-arrow">浏览旅程 &rarr;</span>
      </a>
      <a href="/inspiration" class="cta-card">
        <span class="cta-label">寻找灵感</span>
        <span class="cta-desc">按情绪与偏好探索<br/>属于你的下一站</span>
        <span class="cta-arrow">探索灵感 &rarr;</span>
      </a>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: var(--spacing-section) 2rem;
    background: var(--color-bg-warm);
  }
  .cta-inner {
    max-width: 1000px;
    margin: 0 auto;
  }
  .cta-heading {
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 700;
    text-align: center;
    color: var(--color-text-primary);
    margin-bottom: 3rem;
    letter-spacing: 0.04em;
  }
  .cta-options {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }
  .cta-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    padding: 2.5rem 2rem;
    background: var(--color-bg-card);
    text-align: center;
    transition: opacity 0.2s;
  }
  .cta-card:hover {
    opacity: 0.7;
  }
  .cta-card-primary {
    border: 1px solid var(--color-brand);
  }
  .cta-label {
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: 0.75rem;
  }
  .cta-desc {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    line-height: 1.6;
    flex: 1;
    margin-bottom: 1.5rem;
  }
  .cta-arrow {
    font-size: 0.85rem;
    color: var(--color-brand);
  }

  @media (max-width: 768px) {
    .cta-options {
      grid-template-columns: 1fr;
    }
  }
</style>